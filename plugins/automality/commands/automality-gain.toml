description = "Show automality's measured impact scoreboard (less code, cost, time)"
prompt = "Show the automality gain scoreboard. One shot, change nothing: do not switch mode, write flag files, or persist anything. Render the published benchmark medians (5 everyday tasks; models Haiku, Sonnet, Opus; source benchmarks/ and the README) as plain ASCII bars: Lines of code, no-skill 100% vs automality 6-20% (down 80-94%); Cost, no-skill 100% vs automality 23-53% (down 47-77%); Speed, automality 3-6x faster. The bar length shows the measured range, the label carries the exact figure. These are benchmark medians, not this repo. NEVER print a per-repo savings number: the unbuilt version was never written, so there is no real baseline to subtract from in a live repo. For real per-repo figures, point to /automality-debt (the counted shortcut ledger) and /automality-audit (what is still cuttable). Report only."
